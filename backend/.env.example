# -----------------------------------
# Application
# -----------------------------------

# Port where the backend server runs
PORT=8080

# Environment mode: development | test | production
NODE_ENV=development

# Secret key for JWT signing (change in local .env)
JWT_SECRET=your_jwt_secret_here


# -----------------------------------
# Database connections
# -----------------------------------

# Local PostgreSQL database (development)
# Example: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://<user>:<password>@localhost:5432/<database>?schema=public

# Local PostgreSQL database for automated tests (Supertest)
DATABASE_URL_TEST=postgresql://<user>:<password>@localhost:5433/<test_database>?schema=public

# NOTE:
# Copy this file to ".env" and replace placeholder values before running:
# cp backend/.env.example backend/.env
